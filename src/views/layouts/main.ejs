<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title || "E-Commerce Auth" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/alpinejs@3.13.0/dist/cdn.min.js"
      defer
    ></script>
  </head>
  <body class="bg-gray-100 text-gray-800 min-h-screen">
    <!-- Navbar -->
    <nav class="bg-white shadow p-4 flex justify-between items-center">
      <h1 class="text-2xl font-bold text-blue-600">
        <a href="/">E-Commerce</a>
      </h1>
      <% if (!user) { %>
      <div>
        <a href="/login" class="px-3 text-gray-700 hover:text-blue-600"
          >Login</a
        >
        <a href="/register" class="px-3 text-gray-700 hover:text-blue-600"
          >Register</a
        >
      </div>
      <% } %> <% if (user) { %>
      <div class="flex items-center space-x-4">
        <span class="text-gray-600">Hi, <%= user.username %></span>
        <a href="/logout" class="text-red-500 hover:text-red-700">Logout</a>
      </div>
      <% } %>
    </nav>

    <!-- Main Content -->
    <main class="max-w-5xl mx-auto p-6"><%- body %></main>

    <footer class="text-center text-gray-500 py-4 border-t mt-6">
      <p>&copy; <%= new Date().getFullYear() %> E-Commerce Auth System</p>
    </footer>
  </body>
</html>
